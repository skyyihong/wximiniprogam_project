<!--index.wxml-->
<view class="container">
  <block wx:for="{{btnList}}" wx:key="item">
    <button type="primary" size="mini" bindtap="btnClick" data-path="{{item.path}}"> {{item.context}}</button>
  </block>
</view>